{
  "name": "hangman",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build:words && vite",
    "build": "npm run build:words && vite build",
    "serve": "vite preview",
    "build:words": "node scripts/buildWords.cjs"
  },
  "dependencies": {
    "vue": "^3.2.47",
    "vue-router": "^4.1.6"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "vite": "^6.2.2"
  }
}
